# Build output — regenerated by `node build.js`
dist/

# Secrets. Also listed in worker/.gitignore; repeated here deliberately so a
# secret cannot reach the remote if that file is ever moved or deleted.
.dev.vars
**/.dev.vars
.env
.env.*

# Dependencies and tool state
node_modules/
.wrangler/
.preview/

# macOS
.DS_Store
